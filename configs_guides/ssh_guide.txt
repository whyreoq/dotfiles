1. ls -al ~/.ssh
2. github -> emails -> Keep my email addresses private -> on
3. ssh-keygen -t ed25519 -C "ID+username@://github.com"
4. cat ~/.ssh/id_ed25519.pub
5. github -> ssh and gpg keys -> new ssh key
6. ssh -T git@github.com

